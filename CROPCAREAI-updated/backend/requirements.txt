# --- Web framework ---
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.7.0
pydantic-settings>=2.3.0

# --- Image handling ---
pillow>=10.3.0
numpy>=1.26.0

# --- Primary classifier (MobileNetV3Small) ---
tensorflow-cpu>=2.16.0

# --- Similarity fallback (ResNet50 + cosine similarity) ---
# IMPORTANT: install these from the CPU-only wheel index, NOT plain
# `torch`/`torchvision` from PyPI — the default PyPI wheels bundle full CUDA
# and are 2-4GB each. The CPU-only wheels below are ~150-250MB combined.
# pip install with:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.3.0
torchvision>=0.18.0
scikit-learn>=1.4.0

# --- Testing ---
pytest>=8.2.0
httpx>=0.27.0
